Two brand assets go here. Both are rendered exactly as supplied — never redrawn,
recoloured, or reconstructed.

- `signal-logo.svg` — the SIGNAL product mark, shown in the header.
- `klinekraft-logo.svg` — the KLINEKRAFT maker's mark, shown small in the footer.

Until either file exists, its component falls back to a plain wordmark set in
the display face, so the layout still holds.
